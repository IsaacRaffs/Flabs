{
  "name": "Flabs",
  "version": "1.0.0",
  "description": "This's a plain application learned from curso.dev by Filipe Deschamps",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "npm run services:up && npm run wait-for-postgres && npm run migration:up && next dev",
    "services:up": "docker compose -f infra/compose.yaml up -d",
    "services:stop": "docker compose -f infra/compose.yaml stop",
    "services:down": "docker compose -f infra/compose.yaml down",
    "lint:prettier:check": "prettier --check .",
    "lint:prettier:fix": "prettier --write .",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "npm run services:up && concurrently --names next,jest --hide next -k -s command-jest \"next dev\"  \"jest --runInBand --verbose --setupFiles dotenv/config\"",
    "test:watch": "jest --runInBand --watchAll",
    "migration:create": "node-pg-migrate -m infra/migrations create",
    "migration:up": "node-pg-migrate -m infra/migrations --envPath .env.development up",
    "migration:down": "node-pg-migrate -m infra/migrations --envPath down",
    "wait-for-postgres": "node infra/scripts/wait-for-postgres.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/IsaacRaffs/Flabs.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/IsaacRaffs/Flabs/issues"
  },
  "homepage": "https://github.com/IsaacRaffs/Flabs#readme",
  "dependencies": {
    "async-retry": "^1.3.3",
    "commitlint": "^20.4.2",
    "concurrently": "^9.2.1",
    "dotenv": "^17.3.1",
    "dotenv-expand": "^12.0.3",
    "jest": "^30.2.0",
    "mongodb": "^7.1.0",
    "next": "^16.1.6",
    "node-pg-migrate": "^8.0.4",
    "pg": "^8.18.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@commitlint/config-conventional": "^20.4.2",
    "@eslint/js": "^10.0.1",
    "baseline-browser-mapping": "^2.10.0",
    "eslint": "^10.0.2",
    "eslint-plugin-react": "^7.37.5",
    "globals": "^17.3.0",
    "prettier": "^3.8.1"
  }
}
